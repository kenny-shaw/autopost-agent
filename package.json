{
  "name": "autopost-agent",
  "version": "0.2.4",
  "private": true,
  "description": "Agent-facing CLI and skill package for AutoPost.",
  "type": "module",
  "bin": {
    "autopost": "packages/cli/bin/autopost.js"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "autopost": "node packages/cli/bin/autopost.js",
    "check": "node packages/cli/bin/autopost.js doctor",
    "test": "node tests/cli.test.js && bash scripts/check-skill-sync.sh"
  },
  "license": "UNLICENSED",
  "dependencies": {
    "yaml": "^2.8.0"
  }
}
