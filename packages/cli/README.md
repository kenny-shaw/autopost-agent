# @kennyshaw/autopost

Public CLI used by the AutoPost agent skill. It installs and controls the local
AutoPost Runtime, which publishes videos through local platform sessions.

```bash
npx @kennyshaw/autopost@latest setup
npx @kennyshaw/autopost@latest doctor
```

The Runtime keeps account credentials and browser sessions on the user's local
machine.
